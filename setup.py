#!/usr/bin/env python

from distutils.core import setup

setup(name='codebuild-local',
      version='1.0',
      description='Tool for running buildspec.yaml files locally in docker',
      author='Thomas Klinger',
      author_email='thomas.klinger@protonmail.com',
      url='',
      packages=[],
      scripts=['codebuild-local']
)